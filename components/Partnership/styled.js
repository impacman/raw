import styled from 'styled-components';

export const PartnershipCom = styled.div`
  position: relative;
  width: 100vw;
  min-height: 100%;
  background-color: var(--color-black);
  overflow: hidden;
`;
